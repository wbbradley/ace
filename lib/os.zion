export {getenv}

fn getenv(s String) Maybe String {
  let String(sz, _) = s
  let env = __builtin_ffi_1("getenv", sz) as *Char
  if env != null {
    return Just(str(env))
  } else {
    return Nothing
  }
}
