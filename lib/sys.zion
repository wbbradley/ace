module sys @0.1
get posix

# TODO: make module variable initialization deterministic
var args [str]

def exit(code int)
	print("Quitting with code " + code)
	posix.exit(code)

def set_args(argc int32, argv **char)
	var i = 0
	while i < argc
		vector.append(args, __box__(argv[i]))
		i += 1
