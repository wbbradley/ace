module sys @0.1
link module posix

var args [str]

def exit(code int)
	posix.exit(code.raw)

def set_args(argc __int__, argv *__str__)
	i := argc - 1r
	while i >= 0r
		append(args, __box__(argv[i]))
		i = i - 1r
