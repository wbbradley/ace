export {readline}

link "readline"

fn readline(prompt String) String {
  let String(prompt_sz, _) = prompt
  let raw_line = __builtin_ffi_1("readline", prompt_sz) as *Char
  let line = __builtin_ffi_1("zion_dup_free", raw_line)
  return String(line, strlen(line))
}

