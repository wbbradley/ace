export {ToSvg, to_svg, inline_styles}

class ToSvg a {
  fn to_svg(a, Map String String) String
}

fn inline_styles(options Map String String) String {
  return join(" ", "${k}='${v}'" for (k, v) in options)
}
