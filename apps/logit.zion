module logit

get os
get file

fn main()
	print("note: ", NoLF)
	note := input()
	if note != null
		# TODO: note should be unmaybe'd here
		f := open((os.getenv("HOME") or ".") + "/zlogit.txt", "a")
		write(f, note)
		write(f, "\n")
		close(f)
