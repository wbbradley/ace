module _
import tree

fn main()
	t := tree.insert(null, 5)
	t = tree.insert(t, 6)
	t = tree.insert(t, 4)
	t = tree.insert(t, 40)
	t4 := tree.insert(t, 39)
	t = tree.insert(t4, 3)
	print(t4)
	print(t)
