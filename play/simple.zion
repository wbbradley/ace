module _
# test: pass

type JustInt{G} has
	var val int
	var hey G

tag Empty

type Maybe{T} is Empty or JustInt{T}

def do(t Maybe{any Q})
	when t
		is JustInt{any Q}
			pass
		is Empty
			pass

def main(args [str]) int
	do(JustInt(3, "green"))
