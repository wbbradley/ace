module _

type Just{T} has
	var val T

tag Empty

type Maybe{T} is Empty or Just{T}

def do(t Maybe{any T})
	when t
		is Just{T}
			static_print(t)
			print(t.val)
		is Empty
			print("Nothing!")

def main()
	do(Empty)
