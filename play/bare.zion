module _
get sys
get main
get file

def foo()
	var x [int]
	append(x, 2)


def main()
	f := file.open("play/bare.zion", "r")
	var lines [str]
	for line in file.readlines(f)
		append(lines, line)
	lines[0] = lines[0] + " # this must be the place"
	for line in lines
		print(line)
