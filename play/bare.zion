module _
link module sys
link module main
link module file

def foo()
	var x [int]
	append(x, 2)


def main()
	f := file.open("play/bare.zion", ReadWrite)
	lines := readlines(f)
	print(lines)
	runtime.gc()
