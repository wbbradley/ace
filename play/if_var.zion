module _
# test: pass

link def puts(x __str__) __int__

def main()
	# test that Maybe{int} is automatically unboxed for the
	# type of 'x'
	var x int = 0
	if y := x
		puts("0 is falsey!".raw)
	else
		puts("something else".raw)
