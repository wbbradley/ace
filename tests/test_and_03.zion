module _
# test: pass

fn hey(x Either int float) {
    match x {
        Left(x) {
            print(x)
        }
        Right(x) {
            print(x)
        }
    }
}

fn main() {
    hey(Left(0))
    hey(Right(1.0))
}
