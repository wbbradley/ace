module _
# test: skip

fn main() {
    /*
    do {
        f <- open("tests/test_maybe_monad.zion")
        lines <- readlines(f)
        print(lines)
    }
    */
/*
    bind(
        open("tests/test_maybe_monad.zion", "r"), fn (fp) {
        return bind(readlines(fp), fn (lines) {
            print(lines)
        })
    })
    */
}
