module _
# test: skip
# expect: 6

fn sum(xs [int], f fn (x int) int) int {
    var sum = 0
    for x in xs {
        sum += f(x)
    }
    return sum
}


fn main() {
    print(sum([0, 1, 2], fn (x) { return x + 1 }))
}
