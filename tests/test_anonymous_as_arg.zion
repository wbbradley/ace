module _
# test: skip
# expect: 18 

fn unit() int {
    return 0
}

fn unit() str {
    return ""
}

fn sum[T, G](f fn (x T) T, y G, xs [T]) T {
    var sum = unit() as T
    for x in xs {
        sum += f(x)
    }
    return int(sum * y)
}

fn main() {
    print(sum(fn (x) = x + 1, 3.0, [0, 1, 2]))
}
