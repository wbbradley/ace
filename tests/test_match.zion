data Numbers x z {
    One(x, x)
    Two(z, z)
    Three(x, z)
}

fn foo() => 3

fn main() => match Three(foo(), "hey") {
    One(a, _) => ExitError(a)
    Two(_, _) => ExitOK
    Three(_, _) => ExitOK
}
