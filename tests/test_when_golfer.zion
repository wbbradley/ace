module when_contact

type Golfer has
	var first str
	var last str

type Athlete is Golfer

def recognize_golfer(athlete Athlete)
	when athlete is Golfer
		if golfer.first == "Arnold" and golfer.last == "Palmer"
			print("RIP")
	else
		print("who are you?")

def main()
	recognize_golfer(Golfer("Arnold", "Palmer"))
