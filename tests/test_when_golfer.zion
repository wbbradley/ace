module when_contact
# test: pass

type Golfer has
	var first str
	var last str

type Athlete is Golfer

def recognize_golfer(athlete Athlete)
	when athlete is Golfer
		if athlete.first == "Arnold"
			if athlete.last == "Palmer"
				print("RIP")
			else
				print("You are alive")
		else
			print("You are alive")
	else
		print("who are you?")

def main()
	recognize_golfer(Golfer("Arnold", "Palmer"))
