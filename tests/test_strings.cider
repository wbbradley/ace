# test: pass

fn main() {
    assert("" == "")
    assert("a" == "a")
    assert("b" == "b")
    assert("ab" == "ab")
    assert("abc" == "abc")
    assert("cba" != "abc")
    assert("dabc" != "abcd")
    assert("dacb" != "abcd")
    assert("dcab" != "abcd")
    assert("dcba" != "abcd")
    assert("dbca" != "abcd")
    assert("dbac" != "abcd")
    assert("a" != "")
    assert("" != "a")
    assert("a" != "A")
    assert("A" != "a")
    # TODO: assert("a" + "a" == "aa")
    print("PASS")
}
