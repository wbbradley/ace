module _

# test: pass

type Val{T} has
	var i T

type One{T} has
	var c T

type Two{T} has
	var c T

type Cross is
	Val{int} or
	One{Cross} or
	Two{float}

# Val <: Cross 
# One <: Cross
# Two <: Cross

def main()
	One(Two(Val(3)))
