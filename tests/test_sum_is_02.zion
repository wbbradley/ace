module _
link module std

# test: pass

type Local has
	var number int

type Intl has
	var country_code int
	var number int

type PhoneNumber is Local or Intl

def main()
	number := Local(100)
	intl_number := Intl(1, 100)
