module _

# test: pass
# expect: a
# expect: b
# expect: c
# reject: d

get itertools

fn main() {
    for x in itertools.take(6, itertools.chain([
            itertools.repeat("a", 3),
            itertools.cycle(["a", "b", "c", "d"])
        ]))
    {
        print(x)
    }
    for (x, y) in itertools.izip(iter(["a", "b", "c"]), iter(itertools.repeat(4))) {
        print("(" + str(x) + ", " + str(y) + ")")
    }
}
