module _
# test: pass

type Relative has
	var value float

type Absolute has
	var value float

type Point is
	Relative or Absolute

# override the bool operator for Point
def __bool__(p Point) bool
	return __true__

def main()
	point := Relative(1.0)
	if point
		pass
