module _
# test: pass

type Point is
	Relative(float) or Absolute(float)

# override the bool operator for Point
def bool(p Point) bool
	return true

def main()
	point := Relative(1.0)
	if point
		pass
