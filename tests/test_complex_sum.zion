module _
# test: pass
# expect: hello

type Ax G has
	var t G
	var x int
	var y float

type Bx G has
	var x int
	var t G
	var y float

type Cx P has
	var i int
	var f float
	var t P

type C T is
    Ax T or
    Bx T or
    Cx T

def main()
	a_int := Ax(4, 5, 2.1)
	b_int := Bx(4, 5, 2.1)
	c_str := Cx(4, 5.2, "hello")
	print(c_str.t)
