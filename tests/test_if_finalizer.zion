module _
link module file
# test: pass
# expect: opened tests/test_if_finalizer.zion as fd

def main()
	filename := "tests/test_if_finalizer.zion"
	if f := file.open(filename, file.ReadOnly)
		print("opened " + filename + " as fd " + str(f.fd))
	else
		print("unable to open " + filename)
