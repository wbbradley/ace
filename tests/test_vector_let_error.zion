module _

fn main() {
    xyz := new [int]
    for i in range(0, 100, 20) {
        append(xyz, i)
    }
    print(xyz)
    abc := new [int]
    # error: the left-hand side of this assignment is not a ref
    xyz = abc
}
