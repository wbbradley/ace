# test: skip

fn main() {
    # NB: this needs support for Char in pattern matching
    match ('a', 1, 'c') {
        (a, b, c) {
            assert(a == 'a')
            assert(b == 1)
            assert(c == 'c')
        }
    }
    __builtin_pass_test
}
