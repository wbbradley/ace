module when_fruits
# test: skip

type Fruit is
	Apples(int) or
	Bananas(float)

def how_many(f Fruit) int
	when f
		is Apples(x)
			return x
		is Bananas(x)
			return int(x)

def main()
	how_many(Apples(3303))
	how_many(Bananas(3.03))
