module _
# test: pass

type Data has
	var value int
	var node ListNode

tag Done

type ListNode is Data or Done

type HList has
	var l ListNode

def __getitem__(l HList, index int) int
	return 42

def main()
	l := HList(Data(12, Done))
	l.l
