module _
# test: pass

type Data has
	var value int
	var node ListNode

tag Done

type ListNode is Data or Done

type List has
	var l ListNode

def __getitem__(l List, index int) int
	return 42

def main()
	l := List(Data(12, Done))
	l.l
