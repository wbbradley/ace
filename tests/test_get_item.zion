module _
link module std
# test: pass

type ListNode is Data(int, ListNode) or Done

type List has
	var l ListNode

def __getitem__(l List, index int) int
	return 42

def main()
	l := List(Data(12, Done))
	l.l
