module _

fn takes(x) {
    # error: 7:9.* irrefutable predicates are restricted to non-keyword symbols
    static_print(x)
    match x {
        int {
            print(x)
        }
    }
}

fn main() {
    let y int? = Just(12)
    x := exists(y) ? "hey" : "joe"
    takes(x)
}
