module _

type Cartesian{T} has
	var x T
	var y T

type Radial{T} has
	var radius T
	var theta T

type Point{T} is
	Cartesian{T} or
	Radial{T, T}

def main()
	# error: no function found named __getitem__
	var p Point{float} = Cartesian(2.4, 1.0)
	print(p[0])
