module _

# test: pass
# expect: 4

[global]
fn str[L, R](either Either L R) str => match either {
    Left(left) => str(left)
    Right(right) => str(right)
}

fn left_or_right(a, b, cond) any {
    if cond {
        return Left(a)
    } else {
        return Right(b)
    }
}

fn main() {
    print(left_or_right(4, "shoe", true))
}

