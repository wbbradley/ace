module _
get main
# test: pass
# expect: pass

def main()
	var x *int8 = 0 as *int8
	assert(not (null != x))
	assert(null == x)
	assert(not (x != null))
	assert(x == null)
	x = 100 as *int8
	y := 200 as *int8
	assert(not (null == x))
	assert(null != x)
	assert(not (x == null))
	assert(x != null)
	assert(x != y)

	assert(null >= null)
	assert(not (null >= x))
	assert(x >= null)
	assert(x >= x)
	assert(y >= x)
	assert(not (x >= y))

	assert(not (null > null))
	assert(not (null > x))
	assert(x > null)
	assert(not (x > x))
	assert(y > x)
	assert(not (x > y))

	assert(not (null < null))
	assert(null < x)
	assert(not (x < null))
	assert(not (x < x))
	assert(not (y < x))
	assert(x < y)

	assert(null <= null)
	assert(null <= x)
	assert(not (x <= null))
	assert(x <= x)
	assert(not (y <= x))
	assert(x <= y)

	y = x

	assert(x == y)
	assert(not (x != y))
	assert(x <= y)
	assert(y <= x)
	assert(x >= y)
	assert(y >= x)
	assert(not (y < x))
	assert(not (x < y))
	assert(not (x > y))
	assert(not (y > x))

	print("pass")
		
