module _
get main
# test: pass
# expect: pass

def main()
	var x *int8_t = 0r as *int8_t
	assert(not (null != x))
	assert(null == x)
	assert(not (x != null))
	assert(x == null)
	x = 100r as *int8_t
	y := 200r as *int8_t
	assert(not (null == x))
	assert(null != x)
	assert(not (x == null))
	assert(x != null)
	assert(x != y)

	assert(null >= null)
	assert(not (null >= x))
	assert(x >= null)
	assert(x >= x)
	assert(y >= x)
	assert(not (x >= y))

	assert(not (null > null))
	assert(not (null > x))
	assert(x > null)
	assert(not (x > x))
	assert(y > x)
	assert(not (x > y))

	assert(not (null < null))
	assert(null < x)
	assert(not (x < null))
	assert(not (x < x))
	assert(not (y < x))
	assert(x < y)

	assert(null <= null)
	assert(null <= x)
	assert(not (x <= null))
	assert(x <= x)
	assert(not (y <= x))
	assert(x <= y)

	y = x

	assert(x == y)
	assert(not (x != y))
	assert(x <= y)
	assert(y <= x)
	assert(x >= y)
	assert(y >= x)
	assert(not (y < x))
	assert(not (x < y))
	assert(not (x > y))
	assert(not (y > x))

	print("pass")
		
