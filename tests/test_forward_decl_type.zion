module _ @1.0
# test: pass

# type names can be given as return values, and can be referenced before they
# are declared.
fn bar() Z {
    return I(3)
}

# Finally define Z to be int
type Z is {
    I(s int)
    F(f float)
}

fn main() {
}
