# test: pass

struct MutableVector {
    x var Float
    y var Float
}

instance Str MutableVector {
    fn str(mv) {
        return "<${mv.x}, ${mv.y}>"
    }
}

fn main() {
    var x = 1.2
    var y = 2.3
    let mv = MutableVector(var x, var y)
    mv.x = 2.09
    print(mv.x)
    print(mv)
    print("PASS")
}
