module _
link module std

# test: pass

type BigBox{T} has
	var t T

type Crate{P, Q} has
	var p P
	var q Q

type Box{X, Y} is
	BigBox{X} or
	Crate{X, Y}

def main()
	x := BigBox("yay this works")
	y := Crate(1, x[0])
	std.print(x[0])
	std.print(y[0])
	std.print(y[1])
