module _

# test: pass

type BigBox{T} has
	var t T

type Crate{P, Q} has
	var p P
	var q Q

type Box{X, Y} is
	BigBox{X} or
	Crate{X, Y}

def main()
	x := BigBox("yay this works")
	y := Crate(1, x.t)
	print(x.t)
	print(y.p)
	print(y.q)
