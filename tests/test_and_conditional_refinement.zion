module _
# test: pass
# expect: cur_line : str?
# expect: foobar : str

def maybe_a_line() str?
	return null

def main()
	let cur_line = maybe_a_line()
	static_print(cur_line)
	when cur_line is
		Just(line)
			if line == "something"
				foobar := line
				static_print(foobar)
