module _

# test: pass
# expect: 10

fn main() {
    fn[] sum_up(i int, n int) int {
        if i == n {
            return i
        } else {
            return i + sum_up(i+1, n)
        }
    }
    print(sum_up(0, 4))
}

