# test: pass

newtype Bird = Bird(String, Int)

fn main() {
    let birds = [Bird("Larry", 0),
                 Bird("Curly", 9001),
                 Bird("Moe", 12)]
    for bird /* Bird(name, age) */ in birds {
        let Bird(name, age) = bird
        print("${name} is ${age} year${age == 1 ? "" : "s"} old.")
    }
    print("PASS")
}
