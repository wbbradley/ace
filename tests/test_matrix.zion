# test: pass
# expect: V2.7.0.*-0.8.*1.0

import math {identity}
import matrix {Matrix3, translation2d, scale2d, V3, multiply_vec3}

fn main() {
  let i = identity()
  print(i)

  let dx = 1.5
  let dy = -3.4
  let start = V3(2.0, 3.0, 1.0)
  let t = translation2d(1.5, -3.4)
  let s = scale2d(2.0, 2.0)
  print(multiply_vec3(s * t, start))
  print("PASS")
}
