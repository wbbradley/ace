module _

# test: pass
# expect: 345
# expect: 213141

get itertools {map, join}

fn main() {
    g := [2, 3, 4]
    print(join(",", map(g, fn (a) => a + 1)))
    h := ["2", "3", "4"]
    print(join(",", map(h, fn (a) => a + 1)))
}
