module _
# test: pass
# expect: Empty
# expect: Just(3)

fn do(t Maybe any T) void {
    match t {
        Just(val) {
            print("Just(" + val + ")")
        }
        Empty {
            print("Empty")
        }
    }
}

fn main() {
    do(Empty)
    do(Just(3))
}
