module _
# test: pass
# expect: Just
# expect: 3
# expect: Empty

def do(t Maybe any T) void
	when t is
		Just(val)
			print("Just")
			print(val)
		Empty
			print("Empty")

def main()
	do(Empty)
	do(Just(3))
