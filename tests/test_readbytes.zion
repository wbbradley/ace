module _
# test: pass
# expect: ҂༜҂༜҂༜҂༜҂༜҂༜

get sys

def main() {
    match! readbytes(open("tests/test_readbytes.zion", "rt")) {
        Just(result) {
            match! decode(result, "utf8") {
                Just(line) {
                    print(line)
                }
            }
        }
    }
}
