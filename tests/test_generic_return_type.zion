module _
# test: pass

type D{T} has
	var t T

type E{T} has
	var c C{T}

type C{T} is D{T} or E{T}

def identity(expr C{any X}) any X
	return "asdf"

def main()
	value := E(D("asdf"))
	identity(value)
