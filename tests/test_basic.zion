# fn two() => 2
fn print(x) {}
fn sqr(x) {
    print(x + 2)
    return fn () {
        return "hey"
    }
}


fn plus(x, y) => x + y
fn I(x) => x
fn K(x) => fn (y) => x
fn S(f, g, x) => f(x)(g(x))
fn SK() => S(K)

