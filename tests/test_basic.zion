fn sqr(a) => a * a

class Functor f {
    fn fmap(fn (a) b, f a) f b  
}

fn add(x, y) => x + y
fn twice(f, x) => f(f(x))
fn plus4(x) => twice(add(2))(x)
fn hey() {
    return plus4(3)
}

instance Num Int {
    from_int = id
    + = __add_int
    * = __multiply_int
    - = __subtract_int
    / = __divide_int
}

fn doit(m) {
    return m
}

fn test_doit() {
    return doit(Just("hey"))
}

fn main() {
    return fmap(fn (x) => "str", Just(3))
}
