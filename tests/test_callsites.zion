module _
# test: pass
 
fn myabs(x int) int {
    if x < 0 {
        # for now the minus operator/function needs to be binary
        return -x
    } else {
        return x
    }
}

fn main() {
    myabs(-3)
}
