module _ @1.0
# test: pass
 
def myabs(x int) int {
    if x < 0 {
        # for now the minus operator/function needs to be binary
        return -x
    } else {
        return x
    }
}

def main() {
    myabs(-3)
}
