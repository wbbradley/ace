get math {sqrt}

fn prime_factors(n Int) Int {
    return 0
}



fn gcd(x Int, y Int) Int {
    var x = x
    var y = y
    while True {
        print(concat(concat(str(x), " == "), str(y)))
        if !x == !y {
            return !x
        } else if !x < !y {
            y -= !x
        } else {
            x -= !y
        }
    }
    assert(False)
    return 0
}

fn main() {
    N := 600851475143
    for i in [2..int(sqrt(from_int(N)))+1] {
        print(i)
        print(gcd(N, i))
    }
}
