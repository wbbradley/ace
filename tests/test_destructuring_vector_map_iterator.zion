module _

# test: pass
# expect: 3.0

type Vec has {
    x float
    y float
    z float
}

fn main() {
    # crate a map from ints to Vecs
    var positions [int: Vec]

    # Put some dummy data into the map
    for i in range(10) {
        positions[i] = Vec(float(i), float(i), float(i * 3))
    }

    # Pull out the Z coords from the values
    for (_, (_, _, z)) in positions {
        print(z)
    }
}
