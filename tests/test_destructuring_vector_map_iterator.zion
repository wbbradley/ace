module _

# test: pass
# expect: 3.0

type Vec has {
    x float
    y float
    z float
}

fn main() {
    var positions [int: Vec]
    for i in range(10) {
        positions[i] = Vec(float(i), float(i), float(i * 3))
    }
    for (_, (_, _, z)) in positions {
        print(z)
    }
}
