module _

fn foo() void {
    # error: local type definitions are not yet impl
    type Double has {
        var x float
        var y float
    }

    type Vector2d is { Double }

    x := Double(0.4, 1.3)
}
