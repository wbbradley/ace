# test: pass
# expect: This should be here.

import itertools {take}

fn main() {
    assert(chomp("\n") == "")
    with! let fd = open(__filename__) {
        for line in take(5, readlines(fd)) {
            print(chomp(line))
        }
    }
}
