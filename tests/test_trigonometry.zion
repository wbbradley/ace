# test: pass

import trigonometry {PI, sin, cos, tan, acos, asin, sinh, cosh, tanh, atan, atan2}

fn main() {
    print(PI)
    assert(sin(0.0) == 0.0)
    assert(cos(0.0) == 1.0)
    assert(sin(PI / 2.0) == 1.0)
    assert(sin(-PI / 2.0) == -1.0)
    assert(cos(PI / 2.0) <= 0.00001)
    assert(cos(PI / 2.0) >= -0.00001)
    assert(tan(0.0) == 0.0)
    print("PASS")
}
