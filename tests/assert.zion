module assert

def fail(msg str) {
    print("FAILED: " + msg)
}

def equals(input any, value any, x str) {
    if value != input {
        print("FAILED: " + input + " == " + value + " /*" + x + "*/")
    } else {
        print("PASS")
    }
}
