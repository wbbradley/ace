module _
link module posix
# test: pass

def main()
	i := 0
	l := list(int(posix.rand()), nil)
	i += 1
	while i < 100
		l = list(int(posix.rand()), l)
		i += 1

	# TODO: implement 'for' syntax for iter behavior
	# for item in l
	#     print(item)

	list_step := next(iter(l))
	while list_step
		if step := list_step
			print(step.item)
			list_step = next(step.next_state)
