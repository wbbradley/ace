module _
get list {Cons, Nil, nth}

# test: pass
# expect: 3
# reject: error

fn main() {
    l := Cons(400, Cons(3, Cons(5001, Nil)))
    print(len(l))
    match nth(l, 2) {
        Just(x) {
            print(x)
        }
    } else {
        print("error")
    }
}
