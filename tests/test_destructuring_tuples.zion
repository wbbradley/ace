module _

# test: pass
# expect: 12
# expect: 23
# expect: 1.20
# expect: 3.40

type A is {
    B
    C(_ int)
}

type Vec has {
    x float
    y float
}

fn main() {
    x := (1, (2, 3))
    let (a, (b, c)) = x
    (e, (f, g)) := x

    print(str(a) + str(b))
    print(str(f) + str(g))

    (x, y) := Vec(1.2, 3.4)
    print(x)
    print(y)
}
