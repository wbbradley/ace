module _
# test: pass

def nth(l Node{any T}?, i int) any T?
	if l == null
		return null

	if i == 0
		return l.value
	else
		return nth(l.next, i - 1)

type Node{T} has
	var value T
	var next Node{T}?

def main()
	x := Node(3, Node(3, null))
	y := nth(x, 0)
	if y != null
		print(y)
