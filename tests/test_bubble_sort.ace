# test: pass

import sort {is_sorted, bubble_sort}

fn main() {
    let xs = vector([400000, 390000..5])
    assert(not is_sorted(xs))
    bubble_sort(xs)
    assert(is_sorted(xs))
    print("PASS")
}
