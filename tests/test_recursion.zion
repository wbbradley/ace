module _
# test: pass

def max(mml [any T], mlast any T?) any T?
	if ml := mml
		if last := mlast
			print("Last max is " + str(last))
			if ml.value > last
				return max(ml.next, ml.value)
			else
				return max(ml.next, last)

		print("No max yet")
		return max(ml.next, ml.value)
	return mlast

def fib(n) int
	if n < 2
		return 1
	return fib(n - 1) + fib(n - 2)

def main()
	m := max((3, (2, (4, (1, nil)))), nil)
	if m
		print(m)
	else
		print("No max")
