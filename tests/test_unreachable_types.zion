module _

type Part{T} has
	var i T

type Crate{T, U} has
	var t T
	var u U

type Box{X, Y} is
	Part{X} or
	Crate{X, Y}

def show(b Box{any, any})
	# error: no function found named __getitem__ for callsite b\[0\]
	print(b[0])

def main()
	x := Part("")
	show(x)
