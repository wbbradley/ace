module _
link module std

type Box{X, Y} is
	Part(X) or
	Crate(X, Y)

def show(b Box{any, any})
	# error: no function found named __getitem__ for callsite b\[0\]
	std.print(b[0])

def main()
	x := Part("")
	show(x)
