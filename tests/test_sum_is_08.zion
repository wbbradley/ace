module _
# test: pass

tag Done

type Node T has
	var t T

type Divide T has
	var l Expr T

type Expr T is
	Node T or
	Divide T or
	Done

def compute(expr Expr any X) any X
	# This test is probably ready to be deleted
	return 0

def main()
	e := Node(1)
	d := Divide(e)
	print(compute(d))
